{
  "name": "quillswords",
  "version": "1.0.0",
  "scripts": {
    "build": "bundle exec jekyll build -d dist",
    "vercel-build": "bundle install && bundle exec jekyll build -d dist"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}