{
  "scripts": {
    "build": "bundle install && bundle exec jekyll build -output _site"
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/_site/$1"
    }
  ],
  "buildCommand": "bundle install && bundle exec jekyll build -output _site",
  "outputDirectory": "_site",
  "installCommand": "gem install bundler && bundle install"
}
